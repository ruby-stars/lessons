a = 10
b = 20

# Relational operators - comparison between things
puts a == b # a equals b
puts a != b # a not equal to b
puts a > b  # a bigger than b
puts a >= b # a bigger or equal than b
puts a < b  # a lower than b
puts a <= b # a lower or equal than b
